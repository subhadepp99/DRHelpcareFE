"use client";

// Reuse the existing doctor details page component so the route
// /doctor/[id]/[name]/[department] (or any extra slug) renders the same content
import DoctorProfilePage from "../page";

export default DoctorProfilePage;
